---
framework: jest
licenseTier: small
postMerge: 
  pattern: 
    - "./test/*/*.test.js"
preMerge:
  pattern:
    - "./test/*/*.test.js"
preRun:
     command:
        - npm install
postRun:
     command:
        - node --version
version: 2.0
