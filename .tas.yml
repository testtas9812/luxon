---
framework: jest
tier: small
postMerge: 
  pattern: 
    - "./test/*/*.test.js"
preMerge:
  pattern:
    - "./test/*/*.test.js"
preRun:
     command:
        - npm install
postRun:
     command:
        - node --version
version: 1.0
