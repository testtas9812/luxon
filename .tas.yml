---
framework: jest
tier: large
postMerge: 
  pattern: 
    - "./test/*/*.test.js"
preMerge:
  pattern:
    - "./test/*/*.test.js"
preRun:
     command:
        - npm install
postRun:
     command:
        - node --version
version: 1.0
