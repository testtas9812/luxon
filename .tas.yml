---
framework: jest
tier: large
postMerge: 
  pattern: 
    - "./test/*/*.test.js"
preMerge:
  pattern:
    - "./test/*/*.test.js"
preRun:
     command:
        - npm install
        - npm run build
postRun:
     command:
        - node --version
version: 2.1
